services:
  app:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    image: cmjedunn/logicgate:webapp

    environment:
      DB_URL: #mongo db url here
      PORT: 3000
      NODE_ENV: production
      LOGICGATE_API_TOKEN: SlhwMUVWVWo6ZUljT3hVSVI4em1UNFMwdjU5d09DY1l3RHc5emJiY2Q
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  mongo-data: